# Selection of ODE Solvers

The decision tree to select an appropriate ODE solver in this library
based on the current benchmark results is shown in the figure below.

![ODE Solver Selection](ode_solver_selection.svg)
